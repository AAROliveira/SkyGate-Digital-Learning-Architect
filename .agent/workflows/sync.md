---
description: sync changes with GitHub
---

// turbo
1. Add all changes to git
   `git add .`

2. Commit changes with a message
   `git commit -m "update: automated sync from Antigravity"`

3. Push changes to origin main
   `git push origin main`
